Alice's endpoint acts as the server while Bob's is the client.
Must run the server application first, the password is Abc123!
Then run the client and enter the same password and the server's IP address within the first 10 seconds.
	-127.0.0.1 if running from localhost
Your messages will be prefixed with a "YOU:" before a message, theres will just display their message.
The corresponding ciphertext that is transmitted before decryption is displayed also.